<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Profile</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gray-50">
  <%- include('layout/header') %>

    <main class="min-h-screen bg-gradient-to-br from-indigo-50 to-gray-100 py-12 px-4">
      <div class="max-w-4xl mx-auto">
        <div class="bg-white shadow-xl rounded-2xl overflow-hidden">
          <!-- Header Section -->
          <div class="bg-gradient-to-r from-indigo-600 to-purple-600 h-32"></div>

          <!-- Profile Content -->
          <div class="relative px-6 pb-8">
            <!-- Avatar -->
            <div class="absolute -top-16 left-6">
              <div
                class="w-32 h-32 bg-white rounded-full border-4 border-white shadow-lg flex items-center justify-center">
                <span class="text-5xl font-bold text-indigo-600">
                  <%= user?.email?.charAt(0).toUpperCase() || 'U' %>
                </span>
              </div>
            </div>

            <!-- Content -->
            <div class="pt-20">
              <h1 class="text-3xl font-bold text-gray-800 mb-2">Welcome back!</h1>
              <p class="text-gray-500 mb-6">Here's your profile information</p>

              <!-- Info Cards -->
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <!-- Email Card -->
                <div class="bg-gray-50 rounded-lg p-4 border border-gray-200">
                  <div class="flex items-center space-x-3">
                    <div class="w-10 h-10 bg-indigo-100 rounded-full flex items-center justify-center">
                      <svg class="w-5 h-5 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                          d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                      </svg>
                    </div>
                    <div>
                      <p class="text-xs text-gray-500 uppercase">Email Address</p>
                      <p class="text-sm font-semibold text-gray-800">
                        <%= user?.email || 'Not available' %>
                      </p>
                    </div>
                  </div>
                </div>

                <!-- Member Since Card -->
                <div class="bg-gray-50 rounded-lg p-4 border border-gray-200">
                  <div class="flex items-center space-x-3">
                    <div class="w-10 h-10 bg-purple-100 rounded-full flex items-center justify-center">
                      <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                          d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                      </svg>
                    </div>
                    <div>
                      <p class="text-xs text-gray-500 uppercase">Member Since</p>
                      <p class="text-sm font-semibold text-gray-800">
                        <%= user?.createdAt ? new Date(user.createdAt).toLocaleDateString() : 'Recently' %>
                      </p>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Actions -->
              <div class="mt-8 flex space-x-4">
                <a href="/logout"
                  class="flex-1 bg-indigo-600 text-white py-3 px-4 rounded-lg hover:bg-indigo-700 text-center font-medium transition">
                  Sign Out
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>

    <%- include('layout/footer') %>
</body>

</html>